# hasqrcode needs to be a function
# when container inherit, make sure inheritance comes first in admin 
# update models and push my itself. blank=True, null=True for fkeys + blank=true for tags (should I do null as well?)
# best way to make created date uneditable?
# connecting items to any parent
#inherit generic view class template. pass args?
#attributes or...